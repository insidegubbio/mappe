{
  "buildCommand": "cd gpx && npm install && npm run build && cd ../website && npm install && npm install @sveltejs/adapter-vercel && npm run build",
  "outputDirectory": "website/build",
  "installCommand": "echo skip"
}
